# 测试计划 (TEST_PLAN.md)

以下为 Quick Cookies 项目的测试用例清单。

| 用例 ID | 测试模块 | 测试步骤与预期结果 | 是否涉及回归测试 | 状态 |
| :--- | :--- | :--- | :---: | :--- |
| TC-001 | 全局热键 | **步骤**：运行应用，在 Finder 之外的任何地方按下 `Cmd + Shift + Space`。<br>**预期**：成功触发预览流程（若没有选中文件，显示相应提示）。 | 是 | 待测试 |
| TC-002 | Finder 文件检测 | **步骤**：在 Finder 中不选择任何文件，按下触发快捷键。<br>**预期**：浮动窗口显示“未选择任何文件”或通过 Toast 提示。 | 是 | 待测试 |
| TC-003 | 文本文件预览 | **步骤**：在 Finder 中选中一个普通的 `.txt` 文本文件，按下触发快捷键。<br>**预期**：浮动窗口打开并以纯文本样式正确显示其内容。 | 是 | 待测试 |
| TC-004 | Markdown 渲染 | **步骤**：在 Finder 中选中一个包含标题、圆角表格（交替背景）、本地相对路径图片（如 `docs/images/logo.png`）和内联 HTML 标签的 `.md` 文件，按快捷键预览。<br>**预期**：内容以 GitHub 样式渲染且背景透明；表格呈现精致的 8px 圆角与淡雅的半透明交替行背景；相对路径图片因绑定 Base URL 路径能 100% 成功加载并展示；HTML 标签被清洗且无任何明文代码残留。 | 是 | 需回归 |
| TC-005 | 代码高亮展示 | **步骤**：在 Finder 中选中一个 `.swift` 或 `.py` 文件，按下触发快捷键。<br>**预期**：浮动窗口打开，代码显示对应的语法高亮。 | 是 | 待测试 |
| TC-006 | 编辑模式切换 | **步骤**：在预览窗口中，点击编辑按钮或按下快捷键切换到编辑状态。<br>**预期**：文本变为可编辑，侧边显示行号。 | 是 | 待测试 |
| TC-007 | 编辑保存 | **步骤**：在编辑模式下修改文件内容，点击保存（或按下 `Cmd + S`）。<br>**预期**：修改内容成功写入磁盘，重新读取时内容已更新。 | 是 | 待测试 |
| TC-008 | 设置项变更 | **步骤**：在菜单栏图标中打开设置，修改字体大小，点击保存。<br>**预期**：再次预览文件时，字体大小按设置的参数渲染。 | 是 | 待测试 |
| TC-009 | 辅助功能权限引导 | **步骤**：在没有授予辅助功能权限的情况下启动应用。<br>**预期**：弹出提示框引导用户打开系统偏好设置以授予权限。 | 是 | 已完成 |
| TC-010 | 窗口无边框、Toggle关闭、图层弹簧动画与后台回归测试 | **步骤**：1. 运行应用，确认无 Dock 与系统菜单栏图标；2. 选中 Finder 文件，按快捷键呼出，确认窗口以带内容的丝滑弹簧动画**从该文件图标位置**放大弹出（确认无任何先放大后内容闪现的割裂感，也无两段式隐藏闪现），后面无遮罩且无红绿灯；3. 拖拽预览窗口的任意背景空白处，确认可自由移动窗口；4. 在窗口聚焦状态下直接再次按快捷键，确认窗口以收缩动画**飞回该文件图标原位**；5. 多次连续切换。<br>**预期**：1. 后台 Agent 运行成功；2. 窗口为纯粹无边框设计，支持背景空白拖拽，无红绿灯及细线边框残留；3. 聚焦时按热键可立刻 Toggle 关闭，动画能够**精准与内容渲染合并同步**，首帧渲染在动画前完成，且动画结束后 Layer 完美驻留，无任何白屏、先隐藏后突兀闪现的两段式 Bug，极具物理连续性弹簧质感。 | 是 | 待测试 |
| TC-011 | 业务与提示 Toast 验证 | **步骤**：1. 触发需要弹出 Toast 的业务场景（如加载下一分段失败）；2. 观察屏幕上方是否正常弹出 Toast 提示，文字与图标是否高反差清晰可见，且确认双击 Option 触发预览时不再有任何 DEBUG 创窗大小或位置坐标等测试 Toast 弹出干扰；3. 在 Toast 消失前，连续多次触发 Toast，观察是否只有一个 Toast 在平滑切换且无重叠堆积。 | 是 | 待测试 |
| TC-012 | Finder 文件物理坐标获取验证 | **步骤**：1. 在 Finder 中选中不同位置的文件项目，双击 Option 键触发预览，查看本地 debug_log.txt 中的日志记录；2. 改变 Finder 的视图模式（Icon、List、Column、Gallery）重复测试并校验日志；3. 打开多个 Finder 窗口（其中一些退到后台，保持各自有选中项），在前台活动窗口中切换选中不同的文件，按下快捷键观察日志记录中的坐标是否实时更新为前台文件的坐标。<br>**预期**：本地日志 debug_log.txt 中的 Position 物理坐标能实时、准确地反馈当前前台活动窗口中选中项目的实际屏幕坐标，绝对不会一律显示固定的 (934, 475) 中心坐标，且诊断源提示应显示“Focused”或“Window 遍历成功”（且不再受后台窗口残留 of 旧选中项干扰导致坐标卡住不更新），同时确认整个触发流程中屏幕上没有任何 DEBUG Toast 弹出。 | 是 | 待测试 |
| TC-013 | Finder 路径检测兼容性与熔断压降验证 | **步骤**：1. 在 Finder 中，选中普通代码小文件，按下快捷键进行预览；2. 进入重命名状态，或在空白处、侧边栏上按下快捷键；3. 连续、快速双击 Option 键十次以上，观察系统是否仍然响应并刷新坐标。<br>**预期**：1. 小文件路径获取 100% 成功，不再弹出“未检测到选中文件”错误；在空白处或侧栏会以当前目录兜底；2. 连续快速触发下，系统绝不发生卡顿，且坐标依然能够实时、高精度地显示，表明 Role 剪枝有效减轻了 95% 以上的无用 IPC 通信，完美杜绝了 macOS `accessibilityd` 安全熔断。 | 是 | 待测试 |
| TC-014 | 0ms 秒开、慢慢高亮渲染及 38% : 88% 黄金修长比例测试 | **步骤**：1. 选中一个 66KB 或更大（或几千行）的 Markdown 或代码文件，按下双击 Option 快捷键。<br>2. 观察窗口是否在按下瞬间（0ms）立即弹起，展示骨架屏及加载进度；内容之后是否平滑淡入渲染出高亮文本。<br>3. 检查窗口的长宽尺寸，确认比例为更窄、更长的高置信修长比例（宽度38%，高度88%）。<br>4. 在没有选择任何文件的情况下按下快捷键，观察窗口是否也是 0ms 弹起，并在内部提示“未检测到选中文件，按 Esc 键关闭”。<br>5. 关闭窗口，观察是否能精准缩回至刚才在 Finder 中选中文件的图标位置。<br>**预期**：1. 按键瞬间 0ms 立即弹框，先展示 Loading 骨架屏而后内容淡入，体验绝无卡顿；2. 比例修长精美；3. 错误恢复优雅；4. 关闭飞回图标顺滑准确。 | 是 | 待测试 |
| TC-015 | 亮色/暗色/自适应主题及设置切换测试 | **步骤**：1. 打开设置窗口，并在外观选项中将“外观主题”分别切换至“亮色”、“暗色”和“自适应”；<br>2. 观察已打开的预览窗口，确认窗口的卡片背景色与正文文本颜色是否随之瞬间更新切换（亮色下为高对比度的黑字白底，暗色下为柔和白字深色底，自适应下匹配系统模式）；<br>3. 预览包含语法高亮的代码文件及 Markdown，并在亮色 and 暗色间切换，观察语法着色是否平滑更新（分别正确采用 atom-one-light 和 atom-one-dark，且背景底色没有突兀闪白或闪黑）。<br>**预期**：1. 外观背景和文本颜色实时响应切换，无延迟重载；2. 代码高亮及 Markdown 排版完全适配亮暗模式，颜色清晰可读，不同主题高亮缓存安全隔离。 | 是 | 待测试 |
| TC-016 | 设置页面全新 UI 布局和交互测试 | **步骤**：1. 打开设置窗口，确认其尺寸更紧凑（460x500）且标题栏透明，整体背景融合一体；<br>2. 检查 APPEARANCE（外观）卡片：点击亮色/暗色/自适应分段选择，检查设置页自身及主窗口的主题联动变化；<br>3. 检查 TYPOGRAPHY（排版）卡片：点击字体下拉列表，确认可选；点击字号 Stepper 步进器的 `[ - ]` 和 `[ + ]` 按钮，确认数值在合理范围内递增减，且即时保存；<br>4. 检查 KEYBINDINGS（快捷键）卡片：确认快捷键及常用热键以 Kbd 物理键帽风格渲染；点击“触发快捷键”录制区域，按下新快捷键，确认其成功录制并生效；点击“恢复默认”，确认快捷键被重置为默认的双击 Option；<br>5. 检查 SYSTEM（系统）卡片：切换开机自启动 Switch 开关，确认开关逻辑响应正常。 | 是 | 待测试 |
| TC-017 | 中英双语多语言自适应及热切换测试 | **步骤**：1. 启动应用，检查系统当前语言。若当前 macOS 系统为中文，确认默认展示中文；为英文，则默认展示英文；<br>2. 打开设置窗口，定位到 APPEARANCE（外观）卡片中的“语言 (Language)” Picker；<br>3. 将语言由“简体中文”切换到“English”，确认设置页面内所有文字（包含卡片标题、描述、键帽说明等）即时 0 毫秒刷新为英文；<br>4. 选中 Finder 中某一文件并双击 Option 唤起预览，确认加载中骨架提示与顶部工具栏中文字体即时变化为英文；<br>5. 观察系统右上角 Quick Cookies 🔍 菜单栏，确认其项目已同步更新为 “Open Selected File”、“Settings”、“Quit”；<br>6. 点击“恢复默认设置”，验证语言是否能随系统自适应恢复。 | 是 | 待测试 |
| TC-018 | 编辑器字体兜底与自启动持久化测试 | **步骤**：1. 打开设置窗口，在排版卡片中将“编辑器字体”切换为 “JetBrains Mono”，验证预览及编辑模式（含行号数字）字体能够同步渲染出 JetBrains Mono 的独特字形与连字特征；<br>2. 临时把字体文件从包中移去（模拟未安装/未打包），验证系统是否能安全自动降级到系统默认等宽字体而不崩溃；<br>3. 切换“开机自启动”开关，检查系统设置中的登录项注册状态是否同步注册。 | 是 | 需回归 |
| TC-019 | 应用图标、菜单与工具栏自定义 SVG 图标渲染及自适应测试 | **步骤**：1. 检查构建出的应用包 `QuickCookies.app`，确认在 macOS Finder (访达)、Dock 栏、Activity Monitor (活动监视器) 中是否正确显示了用户最新的面包/贝壳卡通应用图标；<br>2. 观察 macOS 右上角状态栏，确认已替换为面包/贝壳形状的自定义单色状态栏图标 `MenuBarIcon`；<br>3. 点击状态栏图标拉下菜单，确认 “打开选中文件”、“设置” 和 “退出” 菜单项前正确显示对应的自定义 SVG 图标；<br>4. 选中文件触发预览，切换亮暗外观模式并进入编辑模式，确认模式切换按钮（画笔/眼睛）以及保存按钮（向下箭头）均使用自定义 SVG 图标；<br>5. 制造一个获取错误，确认警告页面渲染自定义的 `StatusWarning` 三角感叹号图标；<br>6. 切换 macOS 系统外观模式（亮色 <-> 暗色），确认以上所有 Template SVG 图标能同步自适应反色。 | 是 | 需测试 |
| TC-020 | Finder Sync 右键菜单与免权限降级 | **步骤**：1. 在不授予 Accessibility 权限时，打开 Finder，选中任意 `.md` 文件，右键点击“用 Quick Cookies 预览”；<br>2. 观察主应用是否能够正常唤起，并且窗口动画以当前鼠标位置为原点进行淡入与收缩飞回。 | 是 | 待测试 |
| TC-021 | File Watcher 外部协同编辑冲突提示 | **步骤**：1. 用 Quick Cookies 开启预览某文本文件，并切换进入编辑模式（暂不保存）；<br>2. 在外部编辑器（如 VS Code）保存修改，或在终端里运行 `echo "test" >> file.txt`；<br>3. 回到预览窗口，观察是否弹出警告框。 | 是 | 待测试 |
| TC-022 | SwiftUI 多卡片 Onboarding 引导与彩屑 | **步骤**：1. 清理 UserDefaults 的 `hasCompletedOnboarding` 标识并重启应用；<br>2. 观察 Onboarding 窗口展示，依次测试“下一步”、“上一步”、外观主题热切换与多语言热刷新；<br>3. 前往系统偏好设置开启权限，观察轮询是否在 1 秒内感知并飘落彩色 Confetti 碎屑。 | 是 | 已完成 |
| TC-023 | 不支持文件及文件夹原生 Quick Look 风格矮卡片 0ms 精准起跳预览 | **步骤**：1. 选中一个不受支持的文件（例如 `.zip` 压缩包或二进制 `.o` 文件，或一个常规文件夹，或 `.xcodeproj`），双击 Option 唤起预览；<br>2. 观察预览窗口是否弹出，是否在弹起那一瞬间直接以矮卡片比例 450x320 弹出，确认没有任何先大后小的大小闪烁跳变；<br>3. 检查 UI 中是否成功渲染出该文件/文件夹真实的 Finder 系统图标，且文件名、大小及修改日期均能正确自适应明暗主题；<br>4. 点击“使用默认应用打开”按钮，确认能正常调用系统默认程序打开；<br>5. 验证在异步探测返回“未检测到选中文件”等错误时，窗口是否也会平滑动画收缩至 450x320 并展示精美的自适应提示卡片。 | 是 | 需回归 |
| TC-024 | 图片与 PDF 媒体预览 | **步骤**：在 Finder 中分别选中 `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.bmp`, `.tiff`, `.heic` 图片文件以及 `.pdf` 文件，按下快捷键预览。<br>**预期**：浮动窗口正常打开并呈修长比例（而非矮卡片），图片以原图展示并附带分辨率大小信息卡片，PDF 支持顺畅翻页，防范被物理二进制检测误杀。 | 是 | 需回归 |
| TC-025 | 关闭预览后 Finder 焦点归还 | **步骤**：1. 打开 Quick Cookies 预览某个文件；2. 通过点击 Scrim 背景、按下 Esc 或点击红黄绿按钮关闭预览；3. 在预览窗口完全消失后，直接按下键盘上的上下方向键。<br>**预期**：Finder 能够立刻以高对比度获得焦点（文件选中项在 Finder 中高亮），且方向键操作能无缝移动 Finder 中的文件选中状态，无需用户手动用鼠标点击 Finder 窗口。 | 是 | 已完成 |
| TC-026 | 连续键盘上下键切换预览 | **步骤**：1. 在 Finder 中选中某个代码/文本文件，双击 Option 呼出预览窗口；2. 在预览窗口激活（作为 Key Window）且处于常规预览状态下，按下键盘 `↑` 或 `↓` 方向键；3. 观察 Finder 的选中状态是否在后台切换，且预览窗口是否在 0ms 瞬间平滑无缝加载并显示新选中的文件；4. 按下 `Cmd + E` 进入编辑模式，再按 `↑` 或 `↓` 方向键，验证是否能正常在编辑器里移动光标（不发生 Finder 文件的后台切换）。<br>**预期**：非编辑模式下，按上下键可以流畅在后台切换 Finder 文件并在预览窗同步 0ms 刷新；进入编辑模式后，拦截解除，方向键恢复编辑器内置的光标移动功能。 | 是 | 已完成 |
| TC-027 | Office 办公文档与富文本预览 | **步骤**：在 Finder 中分别选中 `.rtf`, `.doc`, `.docx`, `.xls`, `.xlsx`, `.ppt`, `.pptx` 等 Office 或富文本文件，按快捷键预览。<br>**预期**：浮动窗口正常弹出，以系统级 QLPreviewView 完美显示内容，格式排版不丢失，且工具栏中无法切换到编辑模式。 | 是 | 需回归 |
| TC-028 | Markdown 导出 PDF | **步骤**：1. 预览任意 Markdown 文件；2. 确认工具栏右侧显示“导出 PDF”按钮；3. 点击按钮，确认拉起系统的 NSSavePanel 另存为 PDF；4. 导出后验证是否弹出“PDF 导出成功” Toast 提示；5. 检查导出的 PDF，排版及代码块高亮均符合预期。 | 是 | 需回归 |

